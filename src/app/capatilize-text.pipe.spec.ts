import { CapatilizeTextPipe } from './capatilize-text.pipe';

describe('CapatilizeTextPipe', () => {
  it('create an instance', () => {
    const pipe = new CapatilizeTextPipe();
    expect(pipe).toBeTruthy();
  });
});
